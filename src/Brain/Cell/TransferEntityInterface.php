<?php

declare(strict_types=1);

namespace Brain\Cell;

/**
 * A transfer entity interface.
 *
 * The transfer entity interface is a simple interface that all classes that wise to be serialised and deserialised
 * must implement. There is no dictatorship here as to what methods must exist, instead the interface is present for
 * type casting and later expansion on the project.
 */
interface TransferEntityInterface
{
}
